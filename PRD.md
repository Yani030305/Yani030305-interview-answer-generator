# HireMind AI 面试助手 产品需求文档

> **文档版本**: v2.0
> **更新日期**: 2026-04-21
> **文档作者**: 产品团队
> **状态**: 有效

---

## 1. 产品概述

### 1.1 产品愿景

HireMind AI 面试助手致力于成为求职者最值得信赖的面试准备伙伴，通过AI技术将个性化面试准备的时间从数十小时压缩至数分钟，同时显著提升回答的专业度和针对性。

**核心价值主张**：
- **效率提升**：将传统面试准备效率提升10倍以上
- **个性化程度**：基于用户背景深度定制，非通用模板
- **岗位精准匹配**：通过深度岗位拆解，实现候选人与岗位的精准匹配

### 1.2 目标市场

**细分市场**：面向中国求职者的AI面试准备工具

**目标用户画像**：
| 维度 | 描述 |
|------|------|
| 校招生 | 缺乏面试经验，需要系统性准备各大公司校招面试的应届生 |
| 社招转型 | 有工作经验但需转行者，需要针对性准备新领域面试 |
| 海归群体 | 英文面试准备需求强烈，需要双语回答支持 |
| 社招晋升 | 已有工作经验，寻求职级晋升或管理岗位面试 |

**用户核心痛点**：
1. 面试准备时间成本高（平均准备一个岗位需要20+小时）
2. 通用回答缺乏竞争力
3. 不知道自己简历与岗位的匹配度
4. 不知道如何量化简历中的项目经验
5. 中英文面试准备需要分别投入时间

### 1.3 成功指标

**北极星指标**：
- 用户单次会话生成回答数 ≥ 3个
- 深度岗位拆解功能使用率 ≥ 15%

**辅助指标**：
- 用户7日留存率 ≥ 40%
- 用户月均积分消耗量 ≥ 200积分
- 付费转化率 ≥ 8%
- 用户NPS净推荐值 ≥ 40

---

## 2. 产品价值主张

### 2.1 核心功能矩阵

| 功能层级 | 功能名称 | 用户价值 | 商业价值 | 竞争壁垒 |
|---------|---------|---------|---------|---------|
| 基础层 | 个性化回答生成 | 快速获得定制回答 | 积分消耗 | AI模型能力 |
| 基础层 | 简历解析 | 提取关键信息 | 数据积累 | 解析准确度 |
| 增值层 | 深度岗位拆解 | 理解岗位本质 | 差异化定价 | 岗位分析深度 |
| 增值层 | 批量生成 | 时间节省 | 套餐销售 | 批量处理效率 |
| 社交层 | 问题反馈 | 持续优化 | 用户粘性 | 社区氛围 |

### 2.2 差异化竞争策略

**短期竞争策略**：
- 以岗位拆解功能作为核心差异点，切入"精准匹配"细分市场
- 通过CUHK邮箱赠送策略锁定高价值学生群体

**长期竞争策略**：
- 积累用户回答数据，优化个性化推荐算法
- 建立面试问题库，覆盖主流公司面试题库
- 形成用户回答画像，实现"人岗匹配"智能推荐

---

## 3. 功能需求详述

### 3.1 深度岗位拆解功能（JD Analyzer）

#### 3.1.1 功能概述

深度岗位拆解是产品的核心增值功能，通过AI对岗位描述进行深度语义分析，输出结构化的岗位认知报告，帮助用户快速建立对目标岗位的深度理解。

#### 3.1.2 用户场景

**典型场景一：面试前快速摸底**
```
用户李明看到某大厂"高级前端工程师"岗位，想要快速了解：
1. 这个岗位真正看重什么？
2. 与我的背景是否匹配？
3. 我还欠缺什么能力？

使用路径：粘贴JD → 获取拆解报告 → 对照自身情况
预计节省时间：2小时+
```

**典型场景二：offer比较决策**
```
用户王芳拿到三个不同公司的产品经理offer，需要评估：
1. 哪个岗位发展前景更好？
2. 哪个更符合我的职业规划？
3. 各岗位的薪资天花板？

使用路径：对三个岗位分别拆解 → 对比分析结果 → 辅助决策
```

#### 3.1.3 功能规格

**输入参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| company | string | 是 | 公司名称，用于个性化分析 |
| position | string | 是 | 岗位名称，核心分析对象 |
| city | string | 否 | 工作城市，用于地域薪资参考 |
| jdText | string | 是 | 岗位描述原文，建议500字以上 |

**输出结构**：
```typescript
interface JDAnalysisResult {
  oneLinerJudgment: {
    essence: string;      // 一句话核心判断
    category: string;     // 岗位大类（技术/产品/运营/市场等）
  };
  jdBreakdown: Array<{
    originalText: string;  // 原文描述
    realMeaning: string;   // 真实含义解读
    hiddenRequirement: string;  // 隐性要求
  }>;
  keyCapabilities: {
    required: string[];    // 必备能力（Top 5）
    preferred: string[];   // 加分能力（Top 3）
  };
}
```

**积分策略**：
- 单次拆解消耗：50积分
- 与回答生成积分池打通，支持灵活调配

**性能要求**：
- 平均响应时间：< 5秒
- 95分位响应时间：< 10秒
- 服务可用性：≥ 99.5%

#### 3.1.4 技术实现要点

**AI提示词设计原则**：
1. 角色设定：资深HR总监视角
2. 输出格式：结构化JSON，确保解析稳定性
3. 风格要求：专业、精准、有洞察力
4. 避免泛化：每个判断需有原文依据

**降级策略**：
- 当AI服务响应超时时，自动返回简化版分析
- 简化版包含：一句话判断 + 必备能力列表

### 3.2 个性化回答生成功能

#### 3.2.1 功能概述

基于用户上传的简历文档和选择的面试问题，生成符合用户背景的个性化双语面试回答。

#### 3.2.2 用户场景

**典型场景：简历经历叙述**
```
用户张华面试时被问："请介绍一下你最有成就感的项目"

传统方式：
- 搜索网上模板 → 耗时30分钟
- 套用模板修改 → 缺乏个性化

使用 HireMind：
- 选择"请介绍一下你最有成就感的项目"问题
- 系统读取简历中相关项目经历
- 生成符合张华背景的个性化回答
- 耗时：< 1分钟
```

#### 3.2.3 功能规格

**输入参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| questionId | string | 是 | 问题ID |
| userDocuments | Document[] | 是 | 用户上传的文档 |
| userMode | 'freshman' \| 'experienced' | 是 | 面试模式 |
| language | 'zh' \| 'en' \| 'both' | 是 | 回答语言 |

**输出结构**：
```typescript
interface GeneratedAnswer {
  questionId: string;
  answerZh: string;
  answerEn: string;
  matchHighlights: string[];  // 与简历的匹配点
  suggestedImprovements: string[];  // 建议改进点
}
```

**积分策略**：
- 单次生成：10积分
- 批量生成（全部问题）：999积分（折扣率约50%）

### 3.3 积分系统

#### 3.3.1 积分货币化设计

**设计原则**：
1. 积分作为价值载体，降低用户价格敏感度
2. 积分池打通各功能，消除功能间壁垒
3. 设置优惠套餐，提升客单价和复购率

**积分获取渠道**：
| 渠道 | 积分数量 | 触发条件 |
|------|---------|---------|
| 注册赠送 | 100 | 新用户注册 |
| CUHK邮箱 | 500 | 使用特定邮箱注册 |
| 购买套餐 | 视套餐而定 | 充值支付 |
| 活动赠送 | 不定 | 运营活动 |

**积分消耗场景**：
| 场景 | 消耗积分 | 说明 |
|------|---------|------|
| 回答生成 | 10 | 单次生成 |
| 批量生成 | 999 | 全部问题 |
| 文档导出 | 30 | 单次导出 |
| 岗位拆解 | 50 | 单次拆解 |

#### 3.3.2 套餐设计

| 套餐名称 | 积分数量 | 价格 | 单积分成本 | 目标用户 |
|---------|---------|------|-----------|---------|
| 体验套餐 | 100 | ¥10 | ¥0.10 | 轻度用户 |
| 基础套餐 | 500 | ¥45 | ¥0.09 | 中度用户 |
| 专业套餐 | 1500 | ¥120 | ¥0.08 | 重度用户 |
| 企业套餐 | 5000 | ¥350 | ¥0.07 | 高频用户 |

**定价策略说明**：
- 体验套餐定位拉新，降低首次付费门槛
- 批量生成作为"钩子产品"，带动整体付费渗透
- 企业套餐采用渐进式折扣，锁定高价值用户

---

## 4. 用户体验设计

### 4.1 信息架构

```
HireMind AI
├── 首页（主场景）
│   ├── 文档上传区
│   ├── 岗位描述输入区
│   ├── 问题导航区
│   └── 回答卡片区
├── 认证模块
│   ├── 登录
│   └── 注册
├── 充值模块
│   ├── 积分套餐选择
│   └── 支付流程
├── 历史模块
│   ├── 回答历史
│   └── 岗位拆解历史
├── 账户模块
│   ├── 积分记录
│   └── 订单记录
└── 反馈模块
```

### 4.2 核心交互流程

**首次使用流程**：
```
访问首页 → 强制注册/登录 → 上传简历（可选）→ 选择模式 → 开始使用
```

**岗位拆解标准流程**：
```
输入公司/岗位/JD → 点击"深度拆解" → 查看分析结果 → 查看详情 → 保存到历史
```

**回答生成标准流程**：
```
选择问题 → 点击"生成" → 查看生成结果 → 编辑优化 → 复制使用
```

### 4.3 状态设计

**回答卡片状态机**：
```
[未生成] --点击生成--> [生成中] --成功--> [已生成]
                            |
                            +--失败--> [生成失败] --点击重试--> [生成中]
                            
[已生成] --点击编辑--> [编辑中] --保存--> [已生成]
      |
      +--复制--> [已生成] (提示"已复制")
      |
      +--使用历史--> [已生成]
```

**岗位拆解状态机**：
```
[未拆解] --点击拆解--> [拆解中] --成功--> [拆解成功]
                             |
                             +--失败--> [拆解失败] --点击重试--> [拆解中]
```

---

## 5. 技术架构

### 5.1 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                        客户端                           │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  │
│  │  Next.js │  │ React   │  │ Zustand │  │ Tailwind │  │
│  │   App   │  │         │  │  Store  │  │   CSS   │  │
│  └────┬────┘  └─────────┘  └────┬────┘  └─────────┘  │
│       │                         │                       │
│       └────────────┬────────────┘                       │
│                    │                                     │
└────────────────────┼────────────────────────────────────┘
                     │ HTTPS
┌────────────────────┼────────────────────────────────────┐
│                    │         服务端                      │
│  ┌─────────────────▼────────────────┐                 │
│  │         Next.js API Routes         │                 │
│  │  ┌────────┐ ┌────────┐ ┌───────┐ │                 │
│  │  │ Auth   │ │ Generate│ │Analyze│ │                 │
│  │  │ API    │ │ Answer  │ │ JD    │ │                 │
│  │  └────────┘ └────────┘ └───────┘ │                 │
│  └─────────────────┬─────────────────┘                 │
│                    │                                     │
│  ┌─────────────────▼────────────────┐                 │
│  │         Supabase Layer            │                 │
│  │  ┌────────┐ ┌────────┐ ┌───────┐ │                 │
│  │  │ Auth   │ │Database│ │Edge   │ │                 │
│  │  │        │ │        │ │Functions│ │                │
│  │  └────────┘ └────────┘ └───────┘ │                 │
│  └─────────────────┬─────────────────┘                 │
│                    │                                     │
│  ┌─────────────────▼────────────────┐                 │
│  │        External Services            │                 │
│  │  ┌────────┐ ┌────────┐ ┌───────┐ │                 │
│  │  │DeepSeek│ │ ZPAY   │ │ Email │ │                 │
│  │  │  API   │ │Payment │ │Service│ │                 │
│  │  └────────┘ └────────┘ └───────┘ │                 │
│  └───────────────────────────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

### 5.2 数据模型

#### 5.2.1 核心实体关系

```
User (1) ─────< (N) Document
  │                  │
  │                  │
  │              (N) │
  │                  │
  ├────< (N) AnswerHistory
  │
  ├────< (N) JDAnalysis
  │
  ├────< (N) CreditTransaction
  │
  └────< (N) Order
```

#### 5.2.2 主要数据表

**用户画像表 (profiles)**：
```sql
CREATE TABLE profiles (
  id UUID PRIMARY KEY REFERENCES auth.users(id),
  email TEXT UNIQUE,
  phone_number TEXT UNIQUE,
  full_name TEXT,
  credits INTEGER DEFAULT 100,
  session_id TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);
```

**岗位拆解历史表 (jd_analyses)**：
```sql
CREATE TABLE jd_analyses (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) NOT NULL,
  company TEXT NOT NULL,
  position TEXT NOT NULL,
  city TEXT,
  jd_text TEXT NOT NULL,
  analysis_result JSONB NOT NULL,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

**积分交易表 (credit_transactions)**：
```sql
CREATE TABLE credit_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES profiles(id) NOT NULL,
  amount INTEGER NOT NULL,
  type TEXT NOT NULL CHECK (type IN ('purchase', 'consume', 'bonus', 'refund')),
  description TEXT,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### 5.3 安全性设计

**认证与授权**：
- 所有API请求通过Supabase Auth进行身份验证
- Row Level Security (RLS) 确保用户数据隔离
- 服务端session验证，防止token伪造

**支付安全**：
- ZPAY回调签名双重验证
- 订单状态机控制，防止重复发放积分
- 数据库事务确保积分原子性操作

**接口安全**：
- 频率限制：单IP单接口100次/分钟
- 请求日志完整记录，支持审计
- 敏感操作（如积分扣除）记录操作链路

---

## 6. 迭代路线图

### 6.1 已完成功能（v1.0 - v1.5）

| 功能模块 | 交付版本 | 核心指标 |
|---------|---------|---------|
| 用户注册登录 | v1.0 | 注册转化率 60% |
| 文档上传解析 | v1.0 | 解析成功率 95% |
| 个性化回答生成 | v1.0 | 用户满意度 4.2/5 |
| 积分系统 | v1.1 | 付费率 5% |
| 支付系统 | v1.2 | 支付成功率 99% |
| 历史记录管理 | v1.3 | 功能使用率 70% |
| 批量生成 | v1.4 | 批量使用率 15% |

### 6.2 当前迭代功能（v2.0）

| 功能模块 | 交付版本 | 核心指标 |
|---------|---------|---------|
| 深度岗位拆解 | v2.0 | 功能使用率 ≥ 15% |
| 岗位拆解历史 | v2.0 | 历史查看率 ≥ 40% |
| Token缓存优化 | v2.0 | getSession超时降低 90% |

### 6.3 规划中功能（v2.1 - v3.0）

| 功能模块 | 计划版本 | 价值主张 |
|---------|---------|---------|
| 智能问题推荐 | v2.1 | 基于简历匹配推荐问题 |
| 面试模拟 | v2.2 | AI模拟面试官对话 |
| 行业薪资分析 | v2.3 | 提供市场薪资参考 |
| 简历优化建议 | v2.4 | AI评估简历竞争力 |
| 企业服务 | v3.0 | B端批量面试准备 |

---

## 7. 运营策略

### 7.1 用户生命周期管理

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  获取   │ --> │  激活   │ --> │  留存   │ --> │  变现   │ --> │  推荐   │
└─────────┘     └─────────┘     └─────────┘     └─────────┘     └─────────┘
    │              │              │              │              │
    ▼              ▼              ▼              ▼              ▼
  SEO/广告      新手引导      积分提醒      套餐升级      邀请奖励
  内容营销      首次优惠      功能迭代      企业套餐      社区运营
  CUHK策略      体验套餐      积分补偿      批量优惠
```

### 7.2 关键运营指标

| 阶段 | 指标 | 目标值 | 监控频率 |
|------|------|-------|---------|
| 获取 | 新增用户数 | 周环比 +5% | 每日 |
| 激活 | 首次生成率 | ≥ 70% | 每周 |
| 留存 | 7日留存率 | ≥ 40% | 每周 |
| 变现 | 付费率 | ≥ 8% | 每周 |
| 推荐 | NPS | ≥ 40 | 每月 |

### 7.3 风险应对预案

**技术风险**：
- AI服务宕机：启动降级策略，返回简化版结果
- 数据库故障：启用读写分离，数据副本同步
- 支付异常：暂停支付入口，人工审核处理

**业务风险**：
- 用户流失：启动用户调研，针对性优化
- 竞品跟进：加速迭代，强化数据壁垒
- 政策变化：预留合规调整空间

---

## 8. 附录

### 8.1 术语表

| 术语 | 定义 |
|------|------|
| 北极星指标 | 唯一核心指标，衡量产品成功的关键指标 |
| NPS | Net Promoter Score，净推荐值，用户推荐意愿度量 |
| RLS | Row Level Security，数据库行级安全策略 |
| JD | Job Description，岗位描述 |

### 8.2 文档变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2024-Q4 | 初始版本，定义产品基础框架 | 产品团队 |
| v2.0 | 2026-04-21 | 新增深度岗位拆解功能，更新PRD框架 | 产品团队 |

### 8.3 参考资料

- Next.js 14 Documentation
- Supabase Documentation
- DeepSeek API Documentation
- ZPAY Payment Documentation

---

**文档结束**
